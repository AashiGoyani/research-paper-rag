name: Deploy to AWS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    
    - name: Deploy Frontend to S3
      run: |
        aws s3 cp frontend/indexrag.html s3://research-paper-rec/indexrag.html --cache-control max-age=0
        echo "Frontend deployed to S3"
    
    - name: Package Lambda function
      run: |
        cd lambda
        zip -r lambda-deploy.zip rag_working.py
        echo "Lambda packaged"
    
    - name: Update Lambda function
      run: |
        aws lambda update-function-code \
          --function-name paper-recommender-rag \
          --zip-file fileb://lambda/lambda-deploy.zip
        echo "Lambda function updated"
    
    - name: Deployment Summary
      run: |
        echo "Deployment Complete!"
        echo "Frontend: https://research-paper-rec.s3.amazonaws.com/indexrag.html"